# Public URL of the deployed backend API. Do not include a trailing slash.
VITE_API_URL=https://api.example.com

# Socket.IO usually uses the same backend origin.
VITE_SOCKET_URL=https://api.example.com

# Set to true after the production Socket.IO endpoint is available.
VITE_ENABLE_SOCKET=false
